checkError.sh finds matches in a list and emails matches to user. Run with ./checkError.sh (campusID)
hello.sh does multiple things, run with ./hello.sh
foo.sh runs with ./foo.sh
hello.c must be compiled with gcc, then run with ./hello.c
